# Build output -- generated by `node build.mjs`, attach to GitHub Releases instead
/oaks-lab.html

# ROM-derived data -- not ours to ship, see README "Game data is not ours to ship"
/data/gamedata.json

# Windows shortcut, machine-local
*.lnk

# Editor / OS cruft
.DS_Store
Thumbs.db
